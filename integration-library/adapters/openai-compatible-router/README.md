# OpenAI Compatible Router Adapter

This starter adapter shows how to normalize one config surface across multiple OpenAI-compatible providers.

Use it as the base for:

- provider-specific defaults
- model catalog overrides
- endpoint normalization
- secret-name conventions
