import type { ModuleDefinition } from '../lib/types';

export const modules: ModuleDefinition[] = [
  { id: 'workbench', label: 'Workbench', description: 'Chat, runs, quick actions, and review-ready file context.' },
  { id: 'settings', label: 'Settings', description: 'AI, autonomy, workspace, labs, learning, tools, and diagnostics.' },
];
