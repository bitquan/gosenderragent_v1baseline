name: Desktop Agent CI

on:
  pull_request:
    paths:
      - 'tools/gosenderr-desktop-agent/**'
      - 'tools/vscode-dev-assistant-extension/core/**'
      - '.github/workflows/desktop-agent.yml'
  push:
    branches:
      - '**'
    tags:
      - 'desktop-v*'
  workflow_dispatch:

jobs:
  desktop-smoke:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: tools/gosenderr-desktop-agent/package-lock.json

      - name: Install desktop dependencies
        run: |
          cd tools/gosenderr-desktop-agent
          npm ci

      - name: Smoke checks
        run: |
          cd tools/gosenderr-desktop-agent
          npm run smoke

      - name: Pack (Linux dir smoke)
        run: |
          cd tools/gosenderr-desktop-agent
          npm run pack

  desktop-build-unsigned:
    needs: desktop-smoke
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-latest
            platform: mac
            artifact_name: desktop-mac-unsigned
          - os: windows-latest
            platform: win
            artifact_name: desktop-win-unsigned

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: tools/gosenderr-desktop-agent/package-lock.json

      - name: Install desktop dependencies
        run: |
          cd tools/gosenderr-desktop-agent
          npm ci

      - name: Build unsigned binaries
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: 'false'
        run: |
          cd tools/gosenderr-desktop-agent
          npm run dist:${{ matrix.platform }}

      - name: Upload unsigned artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: |
            tools/gosenderr-desktop-agent/dist/*
          if-no-files-found: error

  desktop-release-mac-signed:
    if: startsWith(github.ref, 'refs/tags/desktop-v')
    needs: desktop-smoke
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: tools/gosenderr-desktop-agent/package-lock.json

      - name: Install desktop dependencies
        run: |
          cd tools/gosenderr-desktop-agent
          npm ci

      - name: Validate Apple signing secrets
        env:
          CSC_LINK: ${{ secrets.MAC_CSC_LINK }}
          CSC_KEY_PASSWORD: ${{ secrets.MAC_CSC_KEY_PASSWORD }}
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_APP_SPECIFIC_PASSWORD: ${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
        run: |
          [ -n "$CSC_LINK" ]
          [ -n "$CSC_KEY_PASSWORD" ]
          [ -n "$APPLE_ID" ]
          [ -n "$APPLE_APP_SPECIFIC_PASSWORD" ]
          [ -n "$APPLE_TEAM_ID" ]

      - name: Build signed + notarized mac binaries
        env:
          CSC_LINK: ${{ secrets.MAC_CSC_LINK }}
          CSC_KEY_PASSWORD: ${{ secrets.MAC_CSC_KEY_PASSWORD }}
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_APP_SPECIFIC_PASSWORD: ${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
        run: |
          cd tools/gosenderr-desktop-agent
          npm run dist:mac

      - name: Upload signed mac artifacts
        uses: actions/upload-artifact@v4
        with:
          name: desktop-mac-signed
          path: tools/gosenderr-desktop-agent/dist/*
          if-no-files-found: error

  desktop-release-win-signed:
    if: startsWith(github.ref, 'refs/tags/desktop-v')
    needs: desktop-smoke
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: tools/gosenderr-desktop-agent/package-lock.json

      - name: Install desktop dependencies
        run: |
          cd tools/gosenderr-desktop-agent
          npm ci

      - name: Validate Windows signing secrets
        shell: bash
        env:
          WIN_CSC_LINK: ${{ secrets.WIN_CSC_LINK }}
          WIN_CSC_KEY_PASSWORD: ${{ secrets.WIN_CSC_KEY_PASSWORD }}
        run: |
          [ -n "$WIN_CSC_LINK" ]
          [ -n "$WIN_CSC_KEY_PASSWORD" ]

      - name: Build signed windows binaries
        env:
          CSC_LINK: ${{ secrets.WIN_CSC_LINK }}
          CSC_KEY_PASSWORD: ${{ secrets.WIN_CSC_KEY_PASSWORD }}
        run: |
          cd tools/gosenderr-desktop-agent
          npm run dist:win

      - name: Upload signed windows artifacts
        uses: actions/upload-artifact@v4
        with:
          name: desktop-win-signed
          path: tools/gosenderr-desktop-agent/dist/*
          if-no-files-found: error
